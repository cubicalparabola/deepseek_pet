node_modules/
dist/
release/

# build/ 整体是「产物与诊断」目录，一律不入库：
#   icon.ico / icon.png / tray*.png      —— 由 npm run icons 生成
#   *.json / *.log / *.txt / *.html      —— 验收与诊断结果
#   bubble/、*-frames/、desktop-burst/ 等 —— 逐帧转储与截图，几十上百 MB
#
# 为什么整体忽略而不是逐条列举：漏一条（例如新加的 *.png 截图）就会误提交
# 几十 MB 的中间数据 —— 实测踩过（`git add -A` 把整目录 500+ 个诊断文件暂存了）。
build/

# 运行期用户设置（首次运行时自动生成，不应提交）
assets/config/settings.json

*.log

.DS_Store
Thumbs.db

# Windows 控制台设备名（调试时误建的文件）
CONOUT$

